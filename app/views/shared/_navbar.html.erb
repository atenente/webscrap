<div class="navbar-top-line bg-dark border-bottom border-secondary">
  <div class="container-fluid text-center">
    <span class="text-light">Webscrap</span>
  </div>
</div>
<nav class="navbar navbar-expand-md navbar-dark bg-dark p-0">
  <div class="container-fluid">
    <% if user_signed_in? %>
      <span><%= link_to t('devise.shared.links.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }%></span>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
            <li>
              <%= link_to t('devise.shared.links.edit_profile'), edit_user_registration_path, class: 'dropdown-item' %>
            </li>
            <li>
              <%= link_to t('devise.shared.links.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'dropdown-item' %>
            </li>
          </ul>
        </li>
      </ul>
    <% end %>
  </div>
</nav>
